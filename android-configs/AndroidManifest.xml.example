<?xml version="1.0" encoding="utf-8"?>
<!--
  Exemplo de AndroidManifest.xml com configurações para WebSocket
  
  INSTRUÇÕES:
  1. Localize seu AndroidManifest.xml em: android/app/src/main/AndroidManifest.xml
  2. Adicione as configurações abaixo no seu arquivo
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== PERMISSÕES NECESSÁRIAS ===== -->
    <!-- Permissão para acessar internet (OBRIGATÓRIO para WebSocket) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permissão para verificar estado da rede (recomendado) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permissão para acessar WiFi (recomendado) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Se precisar de localização da rede (opcional) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        
        <!-- ===== CONFIGURAÇÕES IMPORTANTES ===== -->
        
        <!-- 1. Referenciar arquivo de segurança de rede -->
        android:networkSecurityConfig="@xml/network_security_config"
        
        <!-- 2. Permitir tráfego cleartext (IMPORTANTE para WS) -->
        android:usesCleartextTraffic="true"
        
        <!-- 3. Habilitar hardware acceleration (melhora performance) -->
        android:hardwareAccelerated="true">

        <!-- Activity principal -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>

        <!-- Provider para WebView (se aplicável) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>

<!--
CHECKLIST DE CONFIGURAÇÃO:
[ ] Permissões INTERNET, ACCESS_NETWORK_STATE adicionadas
[ ] android:networkSecurityConfig configurado
[ ] android:usesCleartextTraffic="true" adicionado
[ ] Arquivo network_security_config.xml criado em res/xml/
[ ] App recompilado após mudanças
[ ] Testado em dispositivo real (não apenas emulador)

TROUBLESHOOTING:
- Se ainda não conectar, verificar logs com: adb logcat | grep -i websocket
- Testar se IP é acessível: ping 10.200.0.184
- Verificar se porta 8765 está aberta no firewall
- Confirmar que o servidor WebSocket está rodando
-->

